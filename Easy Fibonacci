#include <iostream>
using namespace std;
int main()
{
	int n; cin >> n;
	
		long  fi = 0, se = 1,sum=0;
	
		if (n == 1) {
			cout << "0";
			return 0;
		}
	cout << fi <<" "<< se<<" ";
 
	for (int i = 0; i < n - 2; i++) {
		
		sum = fi + se;
		fi = se;
		se = sum;
cout << sum << " ";
	}
 
 
}
